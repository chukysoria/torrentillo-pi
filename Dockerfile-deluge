# https://hub.docker.com/r/linuxserver/deluge
FROM --platform=linux/arm lscr.io/linuxserver/deluge:latest

RUN \
 echo "**** install packages ****" && \
 apk add --no-cache \
	nfs-utils && \
 echo "**** clean up ****" && \
 rm -rf \
	/root/.cache \
	/tmp/*

# add local files
COPY root/ /
